/**
 * Which edition this app is. The Enterprise fork (openfilz-web-ee) keeps its own copy of
 * this file with `true` (resolve merge conflicts here with --ours, like package.json);
 * shared components branch edition-specific copy on it instead of forking whole files.
 */
export const IS_ENTERPRISE = false;
