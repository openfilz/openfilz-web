# Example values — local kind cluster: no auth, nginx ingress without a
# hostname, API reached through the same nginx via path routing.

auth:
  enabled: false

ingress:
  className: nginx

app:
  apiUrl: http://localhost/api/v1
  graphQlUrl: http://localhost/graphql/v1

# image:
#   registry: "localhost:5001"
