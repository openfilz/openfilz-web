apiVersion: v1
kind: Service
metadata:
  name: {{ include "openfilz-web.fullname" . }}
  labels: {{- include "openfilz-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector: {{- include "openfilz-web.selectorLabels" . | nindent 4 }}
